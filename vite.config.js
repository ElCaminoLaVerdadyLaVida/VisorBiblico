import { defineConfig } from 'vite';

// Config mínima a propósito. Si más adelante despliegas en una subcarpeta
// (por ejemplo usuario.github.io/visor-biblico-3d/ en GitHub Pages),
// cambia "base" a esa ruta.
export default defineConfig({
  base: './',
});
